{
  "scripts": {
    "start": "node server.js",
    "build": "react-scripts build",
    "test": "node tests/integration_test.js && node tests/component_test.js",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
